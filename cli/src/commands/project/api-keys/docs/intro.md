<DemoVideo id="supa-project-api-keys" />

Display API keys for the linked project.

Shows your `anon` (public) and `service_role` (secret) keys. Keys are masked by default—use `--reveal` to show full values.