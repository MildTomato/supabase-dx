Skip the interactive prompt and initialize locally without connecting to Supabase Platform. Creates the `supabase/` directory structure with a minimal `config.json` (no `project_id`).

Run `supa init` again later to connect to the platform when you're ready to deploy.