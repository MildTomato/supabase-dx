# Test Two

A clean Next.js app for testing the Supabase CLI.

## Getting Started

1. Run `supa init` to initialize a Supabase project
2. Run `supa dev` to start watching for changes
3. Run `npm run dev` to start the Next.js dev server
